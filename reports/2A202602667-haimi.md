# Individual contribution report

## Thông tin

- Họ và tên: Nguyễn Thị Hải Mi
- Mã học viên: 2A202602667
- Nhóm: THTrueMi
- Repository/branch: `tamnd2004/K4-L3B-RAG-Pipeline-THTrueMi` / `main`

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 3 — Chuẩn hoá Markdown | Convert 3 PDF (MarkItDown) và 5 JSON sang `data/standardized/{legal,news}`; header truy vết (title, URL nguồn, file landing); làm sạch header lặp mỗi trang và mục lục của PDF; chạy lại không tạo bản sao | `src/task3_convert_markdown.py`, 8 file `.md` — commit `6a87ed1` | Done |
| Task 7 — RRF | `rerank_rrf()` gộp theo ID với `sum(1/(k+rank))`, copy item để giữ cosine gốc của input, báo lỗi khi một list có ID trùng | `src/task7_reranking.py` — commit `6d8fdae` | Done |
| Task 8 — PageIndex fallback | Upload 8 tài liệu (news convert Markdown → PDF), cache document ID, timeout cho mọi request, xử lý rate limit, parse `retrieved_nodes` thành `SearchResult` | `src/task8_pageindex_vectorless.py` — commit `6d8fdae` | Done |
| Task 9 — Retrieval pipeline | `retrieve()`: dense + BM25, fuse RRF đúng một lần, fallback theo best dense cosine, giữ hybrid khi PageIndex lỗi/rỗng; `use_reranking=False` cho config dense-only (A/B) | `src/task9_retrieval_pipeline.py` — commit `6d8fdae` | Done |
| Hiệu chỉnh threshold | Đo best dense cosine trên 10 query in-domain và 10 query out-of-domain, chọn `SCORE_THRESHOLD = 0.65` | Hằng số và ghi chú trong `src/task9_retrieval_pipeline.py` | Done |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Chọn fallback threshold 0.65, so với best cosine gốc của dense (không dùng RRF score).
   **Lý do/evidence:** Với `gemini-embedding-001`, 10 query in-domain (EN/VI) có best cosine 0.714–0.880; 10 query out-of-domain 0.505–0.574. 0.65 nằm giữa khoảng trống 0.14. Giá trị mặc định 0.3 không bao giờ kích hoạt fallback vì Gemini cho cả query lạc đề khoảng 0.5.
   **Trade-off:** Query in-domain thấp nhất ("Portfolio playbook dùng để làm gì?", 0.714) chỉ cách ngưỡng 0.064. Ngưỡng chỉ đúng cho corpus và embedding model hiện tại; đổi một trong hai phải hiệu chỉnh lại.

2. **Quyết định:** Làm sạch PDF có chọn lọc ở Task 3: chỉ bỏ dòng mục lục và header chạy (dòng lặp ≥ 5 lần và kết thúc bằng tiêu đề tài liệu), không bỏ mọi dòng lặp.
   **Lý do/evidence:** Header "AWS Prescriptive Guidance … playbook for AWS large migrations" lặp 26–83 lần mỗi file, làm nhiễu chunk và BM25. Nhưng có dòng lặp là nội dung thật ("This task consists of the following steps:", 6 lần).
   **Trade-off:** Vẫn còn đoạn copyright ở đầu và vài dòng footer dạng "Step 1: … 13".

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: `pytest tests/test_acceptance.py` (3 test dữ liệu pass); `pytest tests/test_contracts.py` (14/15 pass, test còn lại thuộc Task 10); 20 query hiệu chỉnh; chạy `retrieve()` thật với "What are the phases of the cutover stage?" và "Công thức nấu phở bò?".
- Kết quả trước/sau nếu có: Query cutover có best dense 0.803 → trả hybrid, top 2 là bài "Cutover stage" (~1.6 s). Query phở có best dense 0.530 < 0.65 → thử PageIndex, PageIndex không trả node nào → giữ hybrid để Task 10 từ chối. PageIndex search: từ lỗi 429 xuống 22 request/lần, khoảng 35 s.
- Lỗi đã phát hiện và cách xử lý:
  - PageIndex trả 429 (giới hạn 120 request/phút) khi mỗi document tự poll 2 s/lần → đổi sang submit tất cả rồi poll theo vòng 3 s, chỉ poll document chưa xong, chờ theo `x-ratelimit-reset` khi gặp 429.
  - ChromaDB của nhóm index bằng Gemini (3072 chiều) nhưng `.env` mẫu để `bge-m3` → query sẽ lệch dimension; phải đặt `EMBEDDING_PROVIDER=gemini`.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: PageIndex không trả score nên kết quả được xếp xen kẽ theo rank trong từng document, tài liệu phù hợp nhất chưa chắc đứng đầu. PageIndex chậm (18–35 s/lần), `relevant_content` là đoạn do PageIndex tóm tắt chứ không trích nguyên văn, và endpoint retrieval đã bị PageIndex đánh dấu deprecated.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Chuyển sang chat API mà PageIndex khuyến nghị, và xếp hạng lại kết quả PageIndex bằng cosine với query thay vì xen kẽ theo document.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 2026-09-25
- Tên thành viên: Nguyễn Thị Hải Mi

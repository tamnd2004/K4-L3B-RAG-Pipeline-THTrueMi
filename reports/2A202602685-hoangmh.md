
# Individual contribution report

---

## Thông tin

- Họ và tên: Mai Huy Hoàng
- Mã học viên: 2A202602685
- Nhóm: THTrueMi
- Repository/branch: https://github.com/tamnd2004/K4-L3B-RAG-Pipeline-THTrueMi/main

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Chủ đề của nhóm | Đề xuất và cùng nhóm quyết định chọn bài toán RAG hỏi đáp về hướng dẫn AWS Large Migration, làm cơ sở để thu thập bộ tài liệu thống nhất. | Corpus trong `data/standardized/legal/` và `data/standardized/news/` | Done |
| Golden dataset | Xây dựng 15 câu hỏi có đáp án kỳ vọng, context gốc và vị trí citation; bao phủ câu hỏi keyword, semantic và phân biệt nguồn. | `group_project/evaluation/golden_dataset.json`; commit `263a4b1`; PR #1 | Done |
| Task 10 — Generation có citation | Hoàn thiện sắp xếp chunk, format context có title/source, gọi OpenAI/Gemini/Anthropic theo `.env`, trả đúng `GenerationResult` và safe refusal khi retrieval/provider lỗi. | `src/task10_generation.py`; commit `263a4b1`; PR #1 | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

Mô tả tối đa hai quyết định mà bạn trực tiếp tham gia:

1. **Quyết định:** Chọn chủ đề AWS Large Migration và thiết kế golden set dựa trực tiếp trên 3 playbook cùng 5 bài hướng dẫn đã chuẩn hoá.  
   **Lý do/evidence:** Chủ đề có nhiều loại câu hỏi thực tế; 15 case đều lưu `expected_answer`, `expected_context` và citation theo file/dòng để có thể đối chiếu.  
   **Trade-off:** Bộ câu hỏi bám sát corpus và dễ kiểm chứng, nhưng mới có 15 case và chỉ đại diện cho miền AWS migration hiện tại.

2. **Quyết định:** Task 10 chỉ sinh câu trả lời từ context, hỗ trợ ba provider và trả safe refusal khi không có bằng chứng hoặc provider gặp lỗi.  
   **Lý do/evidence:** `SYSTEM_PROMPT` yêu cầu citation; context có nhãn document/title/source; kết quả luôn giữ schema `answer`, `sources`, `retrieval_source`.  
   **Trade-off:** Cách xử lý này giảm nguy cơ bịa thông tin và tránh làm hỏng luồng ứng dụng, nhưng khi provider lỗi người dùng chỉ nhận câu từ chối thay vì nguyên nhân kỹ thuật chi tiết.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: `pytest -q tests/test_contracts.py`.
- Kết quả: 15/15 contract tests pass; test của Task 10 xác nhận reordering không làm thay đổi input và context chứa title/source. Golden dataset có đủ 15 case theo acceptance requirement.
- Lỗi đã phát hiện và cách xử lý: query rỗng, retrieval không có kết quả hoặc provider lỗi có thể làm vỡ luồng generation; đã thêm kiểm tra đầu vào, bắt lỗi và trả `SAFE_REFUSAL` với `retrieval_source="none"` khi không có nguồn.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: Chưa chạy đánh giá RAGAS/A-B và chưa kiểm chứng end-to-end câu trả lời thật của cả ba provider; report evaluation chung vẫn chưa hoàn tất.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Bổ sung test mock cho từng provider và chạy 15 câu golden set để đo faithfulness, answer relevance, context recall và context precision.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 25/09/2026
- Tên thành viên: Mai Huy Hoàng

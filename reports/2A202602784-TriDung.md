# Individual contribution report

## Thông tin

- Họ và tên: Trần Nguyễn Trí Dũng
- Mã học viên: 2A202602784
- Nhóm: THTrueMi
- Repository/branch: `tamnd2004/K4-L3B-RAG-Pipeline-THTrueMi` / `main`

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Báo cáo evaluation | Hoàn thiện thông tin lượt chạy, cấu hình dense-only và hybrid + RRF, 4 metric RAGAS, latency và chi phí dựa trên dữ liệu đánh giá của nhóm | `reports/RESULT.md`, commit `c3663b4` | Done |
| Phân tích A/B | So sánh điểm tổng và điểm theo nhóm câu hỏi; kiểm tra các case có chênh lệch lớn; kết luận hai cấu hình gần như ngang nhau thay vì xem chênh lệch `+0.0001` là cải tiến | `reports/RESULT.md`, commit `c3663b4` | Done |
| Phân tích lỗi và đề xuất | Xác định lỗi do cắt danh sách qua nhiều chunk, BM25 ưu tiên đoạn lặp từ khoá và evaluator dao động; đề xuất mở rộng chunk lân cận, giảm trọng số BM25 và tăng số lần đánh giá | `reports/RESULT.md`, commit `c3663b4` | Done |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Không kết luận hybrid + RRF tốt hơn dense-only trong lượt đánh giá này.
   **Lý do/evidence:** Điểm trung bình chỉ tăng từ `0.7852` lên `0.7853`. Config B tăng faithfulness `0.0711` nhưng giảm context precision `0.0841`; riêng case 07 và 14 cho thấy evaluator chấm lệch mạnh dù câu trả lời gần giống nhau.
   **Trade-off:** Pipeline sản phẩm vẫn dùng hybrid + RRF để đúng contract Task 9, nhưng báo cáo không xem đó là cải tiến đã được chứng minh.

2. **Quyết định:** Ưu tiên thử mở rộng chunk lân cận trước khi thay đổi toàn bộ chiến lược retrieval.
   **Lý do/evidence:** 4/15 case (01, 03, 10, 15) thiếu đúng chunk kế tiếp chứa phần còn lại của danh sách. Có thể bổ sung `chunk_index + 1` cùng nguồn mà không phải tạo lại embedding.
   **Trade-off:** Context dự kiến dài hơn khoảng 1.5 đến 2 lần, làm tăng input token và có thể giảm context precision.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: đối chiếu 15 case trong `results/generations.json`, `results/scores.json` và `results/summary.json`; phân tích chi tiết case 03, 09 và 15. Lệnh tái lập được ghi trong báo cáo: `python group_project/evaluation/run_evaluation.py generate` và `python group_project/evaluation/run_evaluation.py score`.
- Kết quả trước/sau nếu có: Config A đạt trung bình `0.7852`, Config B đạt `0.7853`. Config B tốt hơn ở nhóm semantic `0.0734`, nhưng kém hơn ở nhóm keyword `0.0851`. Median retrieval tăng từ `0.384s` lên `0.391s`.
- Lỗi đã phát hiện và cách xử lý: phát hiện chunking cắt giữa danh sách, RRF để BM25 đẩy chunk đúng khỏi top 5 ở case 09, và evaluator tạo nhiễu ở case 07/14. Tôi ghi rõ root cause, mức ảnh hưởng và cách kiểm chứng cho từng đề xuất; chưa thay đổi code retrieval trong commit này.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: kết luận dựa trên 15 câu hỏi tiếng Anh và một lượt chạy cho mỗi cấu hình; PageIndex fallback không được kích hoạt, nên chưa đo được query tiếng Việt và nhánh fallback.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: thêm chunk lân cận có giới hạn context, sau đó chạy lại cả hai cấu hình để kiểm tra context recall, context precision, độ dài context và latency.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 2026-09-25
- Tên thành viên: Trần Nguyễn Trí Dũng
